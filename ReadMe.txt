There are two codes. pipeline.ipynp and baseline.py

baseline.py is the code for image harmonization

pipeline.ipynb is the code for the image preprocessing 
you can edit image and run the baseline.py on the pipeline notebook
editing image is implemented by python interaction

If the computing resourse (GPU capability) is not enough, the baseline.py might not working